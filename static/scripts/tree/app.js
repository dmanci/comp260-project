define('tree/app', [
	'jquery',
	'underscore',
	'backbone',
	'd3'
], function(
	$,
	_,
	Backbone,
	D3,
undefined) {
 var TreeApp = Backbone.Model.extend({
	 createTree: function() {
		var app = this;


	 }

 )};

 return new TreeApp();
});
